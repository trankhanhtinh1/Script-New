# 2026-08-05

- Đã đọc và kiểm tra cấu trúc `Nightsharp/sdk/Data/DamageData.h`: cơ sở dữ liệu damage tĩnh gồm 173 champion, dùng các struct `BonusEntry`, `StageEntry`, `SlotEntry`, `ChampionEntry` và tìm kiếm nhị phân theo tên. Xác nhận tên được sắp đúng, không trùng; phát hiện comment chỉ mục bị lặp `[75]` tại Lucian/Lulu nên các nhãn sau lệch 1, nhưng không ảnh hưởng runtime vì mảng thực vẫn đủ 173 phần tử.

- Kiểm tra lại (read-only) nhóm champion N (Naafiri–Nunu) trong `DamageData.h` (dòng 4937–5480) so với `E:\DamageData\json\*.json`. Kết quả: nhiều champion đã lỗi thời (outdated). PASS: Nami Q/W, Nasus Q, Nautilus Q/E, Neeko Q-default/E, Nidalee Q-human, Nilah Q/E, Nocturne Q. OUTDATED: Naafiri Q/W/R (kit cũ), Nami E (base sai), Nami/Nasus/Neeko/Nocturne/Nunu R (bị cắt còn 3 rank), Nasus E (sai cả 2 stage), Nautilus W, Nidalee cougar Q/W/E/R, Nilah R (lưu per-tick thay nuke), Nocturne E (base = 0), Nunu Q (sai scaling type 0.05 bonus), Nunu W/E. Báo cáo lưu tại `GroupN_DamageData_Verification.md`.
- Sửa lỗi script extract: data values nằm ở key `DataValues` (viết hoa D) chứ không phải `mDataValues`. STAT(2)=AD→Scaling 1; STAT(None)+AP ratio→Scaling 3; STAT(12)=bonus max HP→Scaling 4/6.

- HOÀN TẤT cập nhật nhóm N (Naafiri–Nunu, index 87–95) trong `DamageData.h` bằng script `apply_groupN_updates.py` (có backup `DamageData.h.bak_groupN`). Đã sửa: Naafiri (thêm W + sửa Q/E/R, R giờ 0 dmg), Nami (E base + mở rộng R 5 rank), Nasus (E 2-stage + R %maxHP), Nautilus (W 2-stage, giữ R chờ xác minh), Neeko (Q-recast + R 5 rank), Nidalee (cougar Q 2-stage, E, R=0), Nilah (R nuke), Nocturne (E base + R 5 rank), Nunu (Q scaling 4, W snowball + det, E, R 5 rank). Dùng `validate_damagedata.py` để kiểm tra cấu trúc: 173 champion / 565 slot / 723 stage, 0 lỗi, 0 cảnh báo (mọi mảng fixed-size khớp struct). MSBuild bị chặn bởi sandbox (LOLBin) nên dùng validator thay build.
